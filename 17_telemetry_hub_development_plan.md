# 17. RaceSide Telemetry Hub — план поэтапной разработки

Дата: 12 сентября 2026 года. Статус: реализация и локальная проверка в работе; публичная приёмка не завершена.

## 1. Основание и назначение

Источник: «ТЗ для Codex — RaceSide Telemetry Hub.md», разделы 1–85, файл из `/Users/denisandreev/Downloads/`.

SHA-256 исходного ТЗ: `0d6a52ddff3d8fc73f3c5ce1cd2221a2eed584f18390ced00a5f9656279f8d93`.

Документ превращает ТЗ в последовательность небольших проверяемых этапов. Он определяет задачи, зависимости, результаты и условия перехода к следующему этапу. После подготовки плана пользователь поручил выполнить разработку. Публичный запуск пока не выполнен.

Уточнение пользователя от 12 сентября 2026 года: **для Telemetry Hub используем OpenF1**. Это решение имеет приоритет над вариантами источников в исходном ТЗ. В V1 реализуется только OpenF1; независимый provider interface сохраняется для возможной будущей замены без переписывания frontend.

При планировании учтены [PRODUCT.md](PRODUCT.md), [DESIGN.md](DESIGN.md), [SKILLS.md](SKILLS.md), [03_architecture.md](03_architecture.md), [08_race_replay_algorithm.md](08_race_replay_algorithm.md), [10_session_results_sync.md](10_session_results_sync.md), [15_raceside_live.md](15_raceside_live.md) и [16_raceside_live_weekend_sessions.md](16_raceside_live_weekend_sessions.md).

Главная задача продукта: объяснять, **на каких участках круга было выиграно или потеряно время**, и подкреплять выводы телеметрией.

## 2. Границы первой версии и допущения

### Обязательная V1 с подпиской

- Раздел `/telemetry` внутри RaceSide с серверной проверкой подписки.
- Пользователь с активной подпиской получает полный Telemetry Hub.
- Пользователь без подписки после входа получает одно заранее подготовленное демо-сравнение для Гран-при, который прошёл два завершённых этапа назад.
- Гость не получает телеметрические данные и видит действие `Войти и посмотреть демо`.
- Выбор сезона, Гран-при, сессии, пилотов и кругов.
- Все пять режимов: Lap Compare, Best Lap Compare, Session Compare, Teammate Compare, Track Evolution.
- Delta, speed, throttle, brake, gear, RPM, DRS; отсутствие отдельного канала корректно обрабатывается.
- Общая дистанционная ось, синхронный курсор, zoom/pan, интерактивная карта, выбор поворота и карта преимущества.
- Метрики RaceSide, анализ поворотов и фактические rule-based Insights.
- Постоянные ссылки, OpenGraph и серверные share-карточки во всех форматах ТЗ.
- Полноценные desktop/mobile сценарии, обработка ошибок, SEO, события аналитики и контролируемый запуск.

Этапы 0–5 дают внутреннюю версию основного сравнения. Это промежуточная контрольная точка, а не полное выполнение Definition of Done из раздела 84 ТЗ. Первая публичная версия требует завершения этапов 0–11.

### Отложено в соответствии с ТЗ

- Три и четыре одновременные телеметрические линии: поддержать расширяемый контракт, первоначальный UI оптимизировать под две.
- Live-телеметрия в новом Hub: предусмотреть совместимость, интеграцию realtime оставить отдельной задачей. Существующий LIVE продолжает работать.
- Средний темп stint, деградация шин и сравнение stint; сохранение избранного в аккаунте; AI narrative.
- Year-to-year comparison исключён, а не включён в ближайший backlog.

### Принятые допущения

1. Основной путь — `/telemetry`, название раздела — RaceSide Telemetry. Пользовательские тексты остаются русскими; английские названия режимов в плане служат привязкой к ТЗ.
2. Сезоны доступны только при подтверждённом покрытии телеметрией. Наличие спортивного архива 2020–2026 не доказывает наличие телеметрии за все эти годы.
3. Session Compare сравнивает две сессии одного пилота в одном уикенде. Track Evolution может показывать ряд из нескольких сессий, сохраняя две линии в подробном сравнении.
4. Источник V1 — OpenF1 по прямому решению пользователя. Этап 0 проверяет его покрытие, качество и условия использования, а не выбирает другой источник. FastF1 и fallback на сторонний provider не входят в реализацию V1; отсутствие данных OpenF1 обрабатывается явно.
5. Сроки и стоимость сейчас не фиксируются: сначала нужны проверка источников и измерение обработки реальных сессий. Точность и размер покрытия — существенные зависимости плана.
6. Список «популярных сравнений» строится по реальным агрегированным событиям. До накопления статистики показываются последние сессии и предложенные сравнения без выдуманных показателей популярности.
7. Правила доступа, стоимость и рекламная модель определены в `18_subscription_concept.md` и имеют приоритет над прежним требованием публичной телеметрии без регистрации.

## 3. Что уже есть в проекте

Ниже — обнаруженные точки переиспользования в локальном коде на дату плана. Это не подтверждение их состояния на production и не полный аудит реализации.

| Область | Существующая точка | Как использовать |
| --- | --- | --- |
| Каркас сайта | `src/components/racemate/app-shell.tsx`, `src/app/globals.css` | Навигация, тема, типографика и компоновка RaceSide |
| Сессии и участники | `src/data/racemate-repository.ts`, `worker/openf1-session-results.mjs` | Проверить и переиспользовать сопоставление сезонов, сессий, пилотов и классификаций |
| Запись LIVE | `worker/live/persistence.mjs`, `worker/live/normalize.mjs` | Уже предусмотрена запись `live_car_data` и `live_location`; оценить полноту сохранённых сессий |
| LIVE UI | `src/features/live/lib/types.ts`, `src/features/live/components/live-panels.tsx` | Каналы и отображение телеметрии; текущие отсчёты привязаны ко времени и не заменяют нормализованное сравнение кругов |
| Replay и геометрия | `src/features/race-replay/lib/track-geometry.ts`, `src/data/track-models.ts`, `src/data/track-model-types.ts` | Геометрия, progress, сектора и точки поворотов после проверки метрической точности |
| Существующий FastF1 | `worker/fastf1/report_metrics.py`, `worker/fastf1/requirements.txt` | Python-интеграция отчётов с `telemetry=False`; остаётся вне задач Telemetry Hub, который использует OpenF1 |
| Графики | `src/components/ui/chart.tsx`, `recharts` в `package.json` | Сначала проверить существующий движок на синхронизацию, touch и производительность |
| Share и OG | `src/app/api/share-image/drivers/compare/route.ts`, `src/app/api/share-image/grand-prix/[season]/[round]/route.ts`, `src/app/api/og/prediction/[shareSlug]/route.ts` | Паттерны серверного изображения, шрифтов, rate limit и preview; используется `ImageResponse` |
| Изображения | `src/lib/share-image-assets.ts`, `src/lib/prediction-share-fonts.ts` | Переиспользовать загрузку разрешённых ассетов, преобразование и шрифты |
| Кеш | `src/lib/server-ttl-cache.ts` | Короткий серверный кеш; это память процесса, а не долговременное хранилище телеметрии |
| Worker | `worker/index.mjs`, `worker/admin-job-queue.mjs` | Существующий механизм задач, повторов и наблюдения за обработкой |

Важные ограничения переиспользования:

- Координаты и анимация Replay могут быть пригодны для отображения, но их точность для delta и определения торможения нужно доказать отдельно.
- Текущие метаданные поворотов содержат точку `progress`; для анализа дополнительно нужны проверенные границы входа и выхода.
- По уточнению пользователя экран Telemetry Hub не прокручивается и не показывает общий подвал. Навигация RaceSide сохраняется, дополнительные данные открываются во вкладках, настройки — в диалоге.
- LIVE/Replay не переписываются ради Hub. Новые адаптеры подключают общие данные без изменения их публичных контрактов.

## 4. Общая последовательность

| Этап | Результат | Зависит от | Фаза исходного ТЗ |
| --- | --- | --- | --- |
| 0 | Проверенное покрытие OpenF1 и набор контрольных данных | — | Подготовка |
| 1 | Контракты, идентификаторы и версионируемое хранение | 0 | Phase 1 — Data |
| 2 | Загрузка данных через OpenF1Provider | 1 | Phase 1 — Data |
| 3 | Проверенный расчёт по дистанции и delta | 2 | Phase 1 — Data |
| 4 | Внутренний API и правила выбора кругов | 3 | Phase 1 / Phase 2 |
| 5 | Основной экран сравнения двух кругов | 4 | Phase 2 — Core Compare |
| 6 | Интерактивная карта и преимущества по участкам | 3, 5 | Phase 3 — Track |
| 7 | Метрики, повороты и Insights | 6 | Phase 4 — Analytics |
| 8 | Все режимы и быстрые сценарии | 4, 7 | Phase 5 — Modes |
| 9 | Permalink, изображения и preview | 5–8 | Phase 6 — Sharing |
| 10 | Скорость, SEO, аналитика и эксплуатационная готовность | 8, 9 | Сквозные требования |
| 11 | Полная приёмка и управляемый публичный запуск | 0–10 | Definition of Done |

Данные и вычисления проходят проверку до создания аналитического UI. Тесты добавляются в соответствующем этапе, а не откладываются на финальную приёмку.

## 5. Этап 0 — аудит OpenF1 и контрольные данные

**Цель:** до разработки доказать, какие реальные данные можно получать и сравнивать.

Задачи:

- [ ] Проверить соответствие текущих календарных ID, session key, driver ID/номеров и команд; учесть замены пилотов и спринтерские уикенды.
- [ ] Для OpenF1 составить матрицу: сезоны, типы сессий, каналы, координаты, сектора, deleted laps, флаги, pit laps, шины и погода.
- [ ] Проверить реальные ответы, частоту и пропуски измерений, доступ к истории, поведение 429/таймаутов, ограничения загрузки.
- [ ] Отдельно проверить актуальные условия OpenF1 для хранения, перераспространения, коммерческого использования и attribution по первичным документам. Не считать эти условия установленными данным планом.
- [ ] Проверить объём и полноту уже записанных LIVE-данных: наличие кругов и координат ещё не гарантирует полный dataset.
- [ ] Собрать воспроизводимые fixtures: сухая квалификация, гонка с pit laps/SC/VSC, мокрая сессия, спринтерский уикенд, удалённый круг, пропуски каналов. Недоступные реальные крайние случаи дополнить явно обозначенными синтетическими примерами.
- [ ] Выбрать пилотные трассы и сессии; зафиксировать исходные времена, источник, checksum и ожидаемые результаты. Пилотное покрытие не выдавать за покрытие всего календаря.
- [ ] Сделать короткий benchmark существующего Recharts на двух линиях по 500–1500 точек: курсор, zoom/pan, touch, High-DPI. Решение о другой библиотеке принимать только по результатам.

**Артефакты:** отчёт о возможностях OpenF1, список поддерживаемых сессий, контрольные datasets и список установленных ограничений.

**Готово, когда:** хотя бы для одной полной сессии получены сопоставимые круги двух пилотов; ясно, как получить distance и elapsed time; неизвестные условия использования явно выделены; нет неподтверждённых обещаний по сезону или точности.

## 6. Этап 1 — контракты и хранение

**Цель:** отделить поставщика данных от вычислений, API и UI.

Задачи:

- [ ] Описать внутренние модели `TelemetryPoint`, `TelemetryLap`, `TelemetryComparison`, track metadata и provider capabilities.
- [ ] Задать единицы: дистанция в метрах, elapsed time и delta в секундах, абсолютный timestamp с явно определённой единицей/UTC, speed в км/ч, throttle в процентах.
- [ ] Для неизвестных значений использовать `null` и метаданные качества. Не превращать отсутствие brake/DRS в «выключено», а отсутствие RPM — в ноль.
- [ ] Различать boolean и процентный brake; хранить исходное состояние DRS и нормализованное значение с документированным mapping.
- [ ] Определить стабильные ID сессий, участников и кругов. Номер машины не использовать как глобальный ID пилота; принадлежность команде фиксировать для конкретной сессии.
- [ ] В сравнении хранить массив выбранных кругов и reference trace. Каждый круг несёт свой session ID: одно поле `session` недостаточно для Session Compare.
- [ ] Версионировать raw dataset, нормализацию, геометрию, алгоритмы метрик и конфигурацию сравнения; сохранять `provider`, `providerDatasetId`, `sourceLicense`, attribution и checksum.
- [ ] Спроектировать минимальные миграции: каталог датасетов/кругов, версии track metadata, результаты вычислений и сохранённые сравнения. Финальные имена выбрать после проверки существующей схемы.
- [ ] Хранить компактные справочники и индексы в существующей БД; для больших payload определить по benchmark, достаточно ли БД или нужно object storage. Не вводить Redis/отдельный сервис без доказанной необходимости.
- [ ] Описать права публичного чтения подготовленных результатов и серверной записи; закрыть прямой доступ к служебным/лицензионно ограниченным raw datasets.

**Артефакты:** контракты, схема хранения, локальные миграции, правила идентификации и версионирования.

**Готово, когда:** контракт не зависит от внешних ID/формата OpenF1 и допускает будущую замену адаптера; сравнение содержит 2–4 traces без полей, привязанных только к A/B; Session Compare не требует менять схему; локальная проверка миграций и ограничений доступа пройдена. Второй реальный provider для этого не требуется.

## 7. Этап 2 — загрузка, provider layer и кеш

**Цель:** получать данные один раз и повторно использовать сохранённые версии.

Задачи:

- [ ] Создать `TelemetryProvider` с методами meetings, sessions, drivers, laps, telemetry, weather и внутренний `TelemetryService`.
- [ ] Реализовать `OpenF1Provider`, переиспользовав проверенные механизмы авторизации, throttling и сопоставления ID в текущем backend/worker. Не создавать FastF1-адаптер или новый Python-сервис для Hub.
- [ ] Добавить worker-задачи подготовки сессии и кругов через текущую очередь: дедупликация, lease, retry/backoff, timeout, ограничение параллелизма.
- [ ] Загружать вместе контекст круга: шины, stint, track status, pit windows, validity и погодные измерения при наличии.
- [ ] Записывать raw dataset атомарно: частичная загрузка не получает состояние готовности полного набора.
- [ ] При повторном запросе сначала читать сохранённый dataset; после рестарта worker/backend не обращаться заново за уже подготовленной историей.
- [ ] При сбое OpenF1 обслуживать уже подготовленную историю из кеша; для отсутствующего dataset выполнять ограниченные повторы и показывать недоступность. Предусмотреть точку расширения provider fallback из ТЗ, но не подключать второй источник в V1. Не подмешивать данные стороннего provider для заполнения пропусков.
- [ ] Отделить окончательную историю от текущей/предварительной сессии. Для последней нужен короткий TTL, для истории — долговременное хранение с явным обновлением версии при исправлениях.
- [ ] Хранить negative cache для временно недоступных данных с ограниченным сроком; не превращать разовый сбой в постоянное отсутствие телеметрии.

**Артефакты:** OpenF1Provider, worker jobs, кеш, диагностика загрузки и тесты провайдерного контракта.

**Готово, когда:** одна сессия полностью проходит загрузку; повторная подготовка идемпотентна; конкурентные запросы не создают дубли; при отключении OpenF1 сохранённая история открывается, а отсутствующие данные дают корректное состояние недоступности и возможность повторить запрос.

## 8. Этап 3 — нормализация по дистанции и delta

**Цель:** получить корректную общую ось для сравнения разных кругов.

Pipeline: raw → cleaning → lap boundaries → distance → normalization → interpolation → delta → cached normalized dataset. Сегментация поворотов и производные метрики подключаются на этапах 6–7.

Задачи:

- [ ] Удалить дубликаты, некорректные timestamps и физически невозможные значения; отмечать выбросы, шум и gaps, сохраняя исходные данные для диагностики.
- [ ] Определить границы круга, elapsed time от линии старта и закрытие трассы; не допускать скачков через старт/финиш и немонотонной дистанции.
- [ ] Установить приоритет расчёта distance: проверенная дистанция источника либо восстановление по скорости/времени с привязкой к геометрии. Выбранный метод и его погрешность записывать в dataset.
- [ ] Различать пройденную машиной дистанцию и canonical progress трассы. Разные траектории пилотов не должны ошибочно сопоставляться как одинаковая физическая точка только по проценту собственного пути.
- [ ] Интерполировать на общую distance grid; шаг около 5 м — стартовый параметр для проверки, а не обещанная точность исходного измерения.
- [ ] Непрерывные каналы обрабатывать выбранной интерполяцией; gear, boolean brake и DRS — дискретно. Не создавать несуществующие передачи и промежуточные состояния.
- [ ] Не интерполировать через большие разрывы и не экстраполировать недостающий старт/финиш без обоснования. Неполный круг может быть доступен для просмотра, но не давать достоверную full-lap delta.
- [ ] Рассчитать `delta(d) = elapsedA(d) − elapsedB(d)`: отрицательная delta означает, что A быстрее B. Для нескольких traces считать относительно явно выбранной опорной линии.
- [ ] Сверять финишную delta с разностью времён кругов и промежуточную с секторами. Не растягивать искусственно кривую, чтобы скрыть расхождение.
- [ ] Разделить точность расчёта и детализацию отображения: аналитика считается до downsampling; формат для графика готовится отдельно.
- [ ] Зафиксировать численные допуски на контрольных datasets, точность вывода и минимальное качество для публикации метрик.

**Обязательные проверки:** одинаковые круги дают нулевую delta; перестановка A/B меняет знак; дистанция монотонна; совпадение старта/финиша; корректное пересечение границ секторов; пропуски, шум и дискретные каналы; проверка времени на каждом эталоне.

**Готово, когда:** сравнение воспроизводимо на fixtures; ошибка укладывается в записанные допуски; сомнительные данные помечаются или исключаются; шаг сетки не выдаётся за точность сенсора.

## 9. Этап 4 — внутренний API и выбор кругов

**Цель:** frontend получает готовое сравнение и доступные пользовательские варианты.

Задачи:

- [ ] Реализовать внутренние read endpoints из ТЗ: `/api/telemetry/seasons`, `/meetings`, `/sessions`, `/drivers`, `/laps`, `/lap/:id`.
- [ ] Реализовать `POST /api/telemetry/compare`; ответ содержит drivers, laps, distance, channels, delta, track, metrics, corners, insights, качество и attribution. Недоступные блоки явно описываются.
- [ ] Вычисления и обращения к источникам выполнять на сервере/worker. При холодном dataset возвращать состояние подготовки с ID задачи и ограниченным polling, не удерживать долгий HTTP-запрос на время всей загрузки.
- [ ] Проверять входные ID, лимит traces, диапазон дистанции/число точек, принадлежность одному уикенду и совместимость конфигурации трассы. Year-to-year и разные трассы отклонять.
- [ ] Вынести детерминированный выбор Best Lap: завершённый, не удалённый, не invalid, без pit-in/pit-out; отдельно учитывать флаги и наличие пригодной телеметрии.
- [ ] Различать официальный лучший валидный круг и лучший круг, пригодный для анализа. Если первый недоступен, объяснять выбор следующего; не подменять его молча.
- [ ] Зафиксировать политику unusual/unknown track status. Не считать неизвестный статус зелёным; при отсутствии подходящего круга показывать причину и доступный ручной выбор.
- [ ] Ручной просмотр разрешает deleted/invalid и pit laps с предупреждением; неполные/несопоставимые круги не обязаны поддерживать полный аналитический compare.
- [ ] Списки сессий строить из данных с поддержкой Sprint Qualifying/Shootout и сегментов квалификации, без фиксированного набора FP1–FP3.
- [ ] Добавить загрузку детализации выбранного диапазона для zoom; публичные ошибки, rate limit, отмену/игнорирование устаревшего ответа при быстрой смене выбора.

**Готово, когда:** API проходит интеграционные тесты на всех основных типах сессий; источник недоступен, но кешированное сравнение открывается; неверный запрос не запускает дорогую загрузку; отсутствие одного канала не ломает ответ.

## 10. Этап 5 — основной экран и Lap / Best Lap Compare

**Цель:** пользователь самостоятельно сравнивает два круга внутри RaceSide.

Задачи:

- [ ] Добавить `/telemetry`, `/telemetry/[season]/[meeting]`, `/telemetry/[season]/[meeting]/[session]` с обычным каркасом RaceSide; canonical slugs согласовать с существующим каталогом.
- [ ] Создать компактный вход: название, краткое описание, сезон, Гран-при и сессия; по умолчанию предложить последний доступный уикенд и недавние сессии.
- [ ] Реализовать выбор пилотов: имя, код, номер, команда, сезонная фотография при наличии, лучший круг и сортировка по классификации/времени.
- [ ] Реализовать Best Lap, выбор номера, предыдущий/следующий круг, фильтр invalid laps; показывать время, сектора, шины, возраст шин, stint/new-used и состояние трассы при наличии.
- [ ] Создать header и summary с корректным знаком разницы; два напарника должны различаться цветом и дополнительным визуальным обозначением.
- [ ] Построить общий reusable chart component: delta, speed, throttle, brake, gear, RPM, DRS. Каналы не получают семь независимых движков.
- [ ] Реализовать единое состояние cursor distance, zoom range и reference trace; все графики синхронно реагируют на hover, клик, scrubbing, zoom, pan и reset.
- [ ] Разместить каналы во вкладках Overview / Speed / Pedals / Gears / Engine / Track / Analysis с русскими подписями; незавершённые разделы не показывать как готовые функции.
- [ ] На desktop держать основные данные в доступной области экрана с учётом header; на mobile — отдельные вкладки, графики почти на всю ширину и touch-управление.
- [ ] Добавить skeletons, empty, partial, error и retry; сохранять выбранные круги при сбое загрузки.
- [ ] Проверить keyboard focus, управление курсором без мыши, цветонезависимое различение, контраст и reduced motion. Для графика дать читаемое текстовое представление ключевых значений.

**Готово, когда:** пользователь с активной подпиской выбирает сессию и двух пилотов, получает сравнение лучших кругов, меняет круги и читает все доступные каналы на desktop/mobile. Пользователь без подписки после входа может открыть только системное демо. Это контрольная точка внутреннего Core Compare.

## 11. Этап 6 — карта трассы и участки преимущества

**Цель:** связать графики с конкретным участком круга.

Задачи:

- [ ] Создать canonical track metadata с версией конфигурации: длина, старт/финиш, сектора, номера поворотов, distanceStart/apexDistance/distanceEnd и происхождение разметки.
- [ ] Переиспользовать проверенные модели трасс. Не строить каноническую карту заново по случайному кругу и не считать визуальное определение поворота готовой спортивной разметкой.
- [ ] Нормализовать координаты сессий к одной геометрии с контролем направления, rotation/scale, старта/финиша и сложных близко расположенных участков трассы.
- [ ] Отобразить старт/финиш, границы секторов и повороты; при выборе поворота переводить все графики в диапазон entry → apex → exit.
- [ ] Связать карту с курсором: при одной distance обе метки показывают одинаковый progress круга. Это сопоставление участков, а не положение машин в один момент гонки; перекрывающиеся маркеры различимы.
- [ ] Разбить трассу на сегменты и считать локальный выигрыш как изменение delta между границами сегмента. Не окрашивать карту по накопленной delta: лидер круга может проигрывать текущий участок.
- [ ] Ввести нейтральный диапазон с учётом качества данных; при hover показывать участок, пилота и выигранное время.
- [ ] Если разметка поворотов отсутствует, сохранять обзорную карту и zoom по дистанции, но не выдумывать номера/границы и corner insights.

**Проверки:** сумма выигрышей сегментов равна изменению delta за круг в пределах допуска; корректное начало/замыкание карты; соответствие sector/corner mapping эталону; синхронность карты и графиков на touch и desktop.

**Готово, когда:** для опубликованного пилотного покрытия доступны карта преимущества и выбор проверенных поворотов; погрешность геометрии не маскируется декоративной плавностью.

## 12. Этап 7 — метрики RaceSide и Insights

**Цель:** дать проверяемые ответы о причинах разницы времени.

Задачи:

- [ ] Описать формулы, единицы, пороги фильтров и правила недоступности каждой метрики; версионировать параметры вместе с вычислениями.
- [ ] Рассчитать top/average speed, full throttle %, braking distance, ранние/поздние точки торможения, gear usage, DRS usage и sector delta.
- [ ] Указать базу процентов: для Full Throttle и usage по умолчанию доля времени на пригодном интервале, а не доля произвольно ресемплированных точек; покрытие интервала показывать в метаданных.
- [ ] Для каждого размеченного поворота считать entry, minimum, maximum и exit speed, braking point, throttle pickup и time delta; отдельно агрегировать straight/corner delta.
- [ ] Значимое торможение определять через сглаживание, порог и минимальную длительность/дистанцию, устойчивость состояния и hysteresis; микросрабатывания не считать отдельными событиями.
- [ ] Разделить начало открытия газа и достижение полного газа после apex/braking phase. Для полного газа требовать устойчивого достижения порога, а не единичного шумового отсчёта.
- [ ] Корректно сопоставлять несколько торможений в одном повороте, отсутствие brake channel, движение без торможения и повторное открытие газа.
- [ ] Создать rule-based Insights: самый значимый участок выигрыша, преимущество на прямой, разница торможения/выхода и газа. Каждый вывод должен ссылаться на вычисленную метрику и участок.
- [ ] Ограничить число выводов, отсекать шум и дубли. Не утверждать причинность, которую телеметрия не доказывает; не делать LLM-вызов для каждого сравнения.
- [ ] Добавить компактный summary и подробную панель поворота; отсутствие достоверной метрики не блокирует остальные каналы.

**Готово, когда:** все минимальные метрики раздела 18 ТЗ реализованы либо обоснованно недоступны на конкретном dataset; braking/throttle tests устойчивы к шуму; значения summary, карты, Insights и финишной delta согласованы.

## 13. Этап 8 — остальные режимы и быстрые сравнения

**Цель:** завершить все пять режимов и сократить путь к полезному сравнению.

Задачи:

- [ ] **Teammate Compare:** по выбранному пилоту определить команду и другого участника именно этой сессии; автоматически выбрать лучшие пригодные круги. Учесть резервистов, DNS, замену пилота и отсутствие второго доступного участника.
- [ ] **Session Compare:** один пилот, разные сессии одного уикенда; Best Lap или ручной круг для каждой, общий track configuration, контекст шин/секторов/времени и погоды.
- [ ] **Track Evolution:** отдельная analytics page в `/telemetry/[season]/[meeting]/evolution`; ряд FP1 → FP2 → FP3 → квалификация или начало → конец сессии из фактически доступных данных.
- [ ] Определить правила выборки «начала» и «конца» сессии и агрегации: одинаковый пилот/сопоставимая группа при возможности, явно обозначенное сравнение лучших кругов иначе. Не выводить Q3, если источник даёт только всю квалификацию.
- [ ] Показывать изменения lap time, средней/секторной скорости, minimum corner speed, braking points и acceleration zones; track/air temperature, влажность, дождь и ветер — по времени соответствующего круга при наличии.
- [ ] Формулировать результат как наблюдаемое изменение времени кругов. Разные шины, топливо, машины, пилоты и погода не позволяют автоматически объявлять трассу «быстрее на X».
- [ ] Добавить Quick Compare: P1 vs P2, напарники, Pole Battle и переход к Track Evolution. Pole Battle — preset Best Lap Compare по квалификационной классификации, а не новый backend mode.
- [ ] Автоматически предлагать сравнение по детерминированным правилам: квалификация — первые два в классификации; гонка — два пилота с лучшими валидными быстрыми кругами. Явно различать эту сортировку и финишные позиции.
- [ ] Сохранять recent comparisons только для пользователя с активной подпиской; обработать недоступный/повреждённый localStorage и устаревшие данные. При наличии реальных счётчиков показывать популярные сравнения.

**Готово, когда:** все пять режимов доступны на контрольных уикендах; спринт, мокрые сессии и замены пилотов не ломают выбор; ограничения сопоставимости видны пользователю; для простого сравнения не требуется вводить lap number.

## 14. Этап 9 — постоянные ссылки и share-карточки

**Цель:** любое сравнение можно воспроизвести и показать вне сайта.

Задачи:

- [ ] Сохранять comparison config для подписчика и выдавать короткий ID для `/telemetry/compare/[id]`. В record входят режим, конкретные lap IDs, reference trace и версии datasets/алгоритмов; полную телеметрию дублировать необязательно. Просмотр ссылки проверяет подписку и не открывает данные пользователю без доступа.
- [ ] При сохранении Best Lap разрешать выбор в конкретные круги. Повторное открытие ссылки не должно выбирать другой лучший круг после обновления результатов.
- [ ] Для Session Compare хранить сессию каждого круга; для Track Evolution — конкретную выборку и метод агрегации. «Любое comparison» включает эти сценарии.
- [ ] Зафиксировать, какие параметры просмотра сохраняются: выбранный канал, поворот и диапазон. Невалидные параметры не должны ломать базовое сравнение.
- [ ] Определить поведение при исправлении/удалении исходной версии: воспроизводимая сохранённая версия, если её хранение допустимо, либо понятное сообщение о недоступности; без молчаливой подмены данных.
- [ ] Создать share modal с preview, копированием ссылки и скачиванием изображения; использовать системный share при поддержке. Отправку в соцсети автоматически не выполнять.
- [ ] Реализовать пять вариантов: Overview, Telemetry, Track Advantage, Corner Comparison, Insight. Если вариант требует отсутствующей метрики, объяснять его недоступность.
- [ ] Реализовать три размера: **1600 × 900**, **1080 × 1350**, **1080 × 1920**.
- [ ] Рендерить изображения на сервере через проверенные механизмы RaceSide. Графики/карту отрисовывать из того же готового dataset; обычный screenshot страницы не является единственным экспортом.
- [ ] Включить GP, сезон, сессию/сессии, пилотов, времена, final delta, карту, выбранный главный график, insight, логотип RaceSide и `raceside.online`; композицию адаптировать к варианту без перегрузки.
- [ ] Подключить автоматические OG/Twitter previews; хранить кеш изображения по comparison/version/template/size, ограничивать частоту и объём генерации.

**Проверки:** ссылка открывается в новом браузере подписчика без localStorage; пользователь без подписки видит предложение оформить RaceSide Plus, а не данные сравнения; данные подписчика совпадают с исходным экраном; все 15 сочетаний «5 вариантов × 3 размера» просмотрены; длинные имена, кириллица, шрифты, частичные каналы и знаки delta читаются корректно.

**Готово, когда:** сохранённое сравнение воспроизводится, все форматы скачиваются, OG получает содержательную карточку и не требует интерактивного браузерного рендера.

## 15. Этап 10 — производительность, SEO и эксплуатация

**Цель:** подготовить весь раздел к устойчивой публичной работе.

Задачи:

- [ ] Для overview отдавать ориентировочно 500–1500 точек на круг, загружать нужные каналы и детализацию zoom; применять сжатие и downsampling, сохраняющий значимые экстремумы/события.
- [ ] Проверить, что уменьшение количества точек меняет только визуализацию, а метрики и delta остаются рассчитанными на аналитическом dataset.
- [ ] Замерить cold/warm API, время подготовки, размер ответа, память worker, частоту внешних запросов и отзывчивость курсора на целевых desktop/mobile устройствах.
- [ ] Зафиксировать численные бюджеты после исходного benchmark этапа 0 и проверить их перед выпуском. Количество точек само по себе не является доказательством быстрого интерфейса.
- [ ] Проверить ключи кеша справочников, normalized datasets, comparisons и share images: режим, круги, reference trace, диапазон/детализация и версии не должны конфликтовать.
- [ ] Подготовить индексируемые страницы опубликованных сессий с серверным содержимым, title/description/canonical/OpenGraph/Twitter Card и включением в sitemap по правилам проекта.
- [ ] Выборочные comparison pages индексировать по явной политике; служебные/дублирующиеся комбинации не индексировать. Не создавать пустые страницы для неподдерживаемых лет.
- [ ] Добавить `telemetryHub`, `telemetryInsights`, `telemetryShare`, `trackEvolution` через найденный в проекте механизм flags или минимальную серверную конфигурацию. Флаги проверять в routes/API, а не только скрывать кнопки.
- [ ] Добавить события: `telemetry_open`, `telemetry_compare_created`, `telemetry_driver_selected`, `telemetry_lap_selected`, `telemetry_chart_changed`, `telemetry_corner_selected`, `telemetry_share_open`, `telemetry_share_generated`, `telemetry_permalink_created`.
- [ ] Переиспользовать действующий механизм продуктовой аналитики, если он есть; если нет — описать минимальный сбор агрегатов. Не подключать новый внешний сервис автоматически. Не записывать каждый hover как событие.
- [ ] Добавить наблюдаемость: задержки, hit rate кеша, ошибки providers, очередь/повторы, доля partial datasets, ошибки нормализации и image generation, версия и источник для диагностики.
- [ ] Проверить восстановление после рестарта, частичного импорта и сбоя источника; отключение Hub/Share не должно нарушать LIVE, Replay и остальные страницы.

**Готово, когда:** документированы и соблюдены измеренные бюджеты; SEO и events проверены; history не перекачивается на каждый просмотр; flags и диагностика позволяют безопасно ограничить проблемную функцию.

## 16. Этап 11 — полная приёмка и запуск

**Цель:** подтвердить все сценарии публичной V1 и выпускать только проверенное покрытие.

Задачи:

- [ ] Выполнить общую матрицу тестов ниже и все 25 пунктов Definition of Done.
- [ ] Проверить ширины 375, 768, 1024 и 1440 px, короткий desktop viewport, touch, клавиатуру, reduced motion и контраст.
- [ ] Проверить одновременно header, карты, вкладки, legends, summary, share modal и отсутствие горизонтального переполнения страницы.
- [ ] Пройти сценарии no telemetry, partial telemetry, provider unavailable, invalid comparison и cold preparation с последующим успешным повтором.
- [ ] Выполнить регрессию LIVE, Replay, существующего сравнения пилотов, карточек GP и навигации.
- [ ] Зафиксировать таблицу опубликованных сезонов/сессий/трасс, отсутствующих каналов и ограничений точности.
- [ ] Подготовить порядок включения flags: внутреннее тестирование → проверенное покрытие → расширение после приёмки каждого набора.
- [ ] Подготовить возврат к предыдущему состоянию через flags/версию приложения без удаления уже сохранённых datasets и ссылок.
- [ ] Составить отчёт о релизе: что работает, какие проверки выполнены, известные ограничения, способ диагностики и восстановления.

**Готово, когда:** Definition of Done выполнен на опубликованном покрытии; нет скрытых заглушек вместо данных; оставшиеся ограничения названы явно; решение о публичном выпуске относится к отдельной задаче реализации/публикации.

## 17. Сквозные технические правила

### Предполагаемые места новых модулей

Это ориентир для небольших локальных добавлений, а не обязательство заранее создать все файлы:

- `src/features/telemetry/` — типы отображения, состояние взаимодействия и компоненты Hub по существующему feature-паттерну LIVE/Replay.
- `src/app/telemetry/` — страницы и metadata.
- `src/app/api/telemetry/` — внутренний публичный API подготовленных данных.
- `worker/telemetry/` — OpenF1Provider, нормализация, derived metrics и задачи подготовки.
- `supabase/migrations/` — минимальные миграции хранения после этапа 1.
- `src/app/api/share-image/telemetry/` — серверные изображения по существующему паттерну.

Детерминированные вычисления должны иметь одну реализацию на сервере; UI и экспорт используют её результаты. Выбор способа обмена типами между TypeScript и `.mjs` уточнить по существующим паттернам, без создания нового монорепозитория.

### Правила данных и точности

- Никогда не сравнивать круги по абсолютному timestamp вместо distance.
- Не переносить геометрию одного года/конфигурации на другую без проверки совместимости.
- Различать официальный timing, интерполированное время и приблизительную оценку; не обещать миллисекундную точность на неподходящем источнике.
- Не объединять deleted, pit и флаговые круги в «плохие» без сохранения причины; validity и аналитическая пригодность — разные признаки.
- Финишная разница, локальный выигрыш на карте и формулировка Insights должны использовать одну знаковую конвенцию.
- Raw history хранится долговременно, но официальные исправления и обновления алгоритмов создают новые версии. «Практически immutable» не означает запрет исправлений.
- Attribution доступен рядом с анализом и в экспорте, если этого требуют условия конкретного dataset.

### Работа по одному этапу

Перед реализацией этапа прочитать актуальные проектные инструкции, этот план и соответствующие разделы ТЗ; проверить изменения рабочей копии. Выполнять этап небольшими локальными изменениями, затем фиксировать результат и подтверждение его критериев готовности. Не отмечать этап завершённым только по наличию файлов.

При работе с frontend применяются `design-taste-frontend` и `impeccable`; для Next.js — `next-best-practices`, shadcn — `shadcn`, продуктовых текстов — `content-design`, Supabase — соответствующие skills, багов — `systematic-debugging`, браузерной проверки — Browser/Playwright. Сам текущий документ не создаёт frontend, миграции или интеграции.

## 18. Матрица проверок

| Область | Обязательные случаи | Этап |
| --- | --- | --- |
| Provider/ingestion | OpenF1: успех, пустой ответ, 429, timeout, чтение кеша при сбое, повторный запуск, конкурентная подготовка, атомарная готовность | 0–2 |
| Идентичность | Замена пилота, одинаковые номера в разных контекстах, команда в конкретной сессии, DNS, sprint | 1, 4, 8 |
| Нормализация | Дубликаты, timestamp disorder, невозможная скорость, gaps, monotonic distance, старт/финиш, разная частота измерений | 3 |
| Интерполяция/delta | Непрерывные/дискретные каналы, одинаковый круг, смена reference, финиш и сектора в пределах допуска | 3 |
| Выбор кругов | Best Lap, deleted/invalid, unknown status, yellow/red, SC/VSC, wet, incomplete, pit-in/pit-out | 4 |
| Карта | Sector/corner mapping, изменение конфигурации, несовместимые координаты, замыкание, локальный gain/loss | 6 |
| Аналитика | Шум brake, несколько braking events, throttle pickup, отсутствующие каналы, агрегация и согласованность Insights | 7 |
| Режимы | Все 5 режимов, sprint weekend, несколько сессий, разные условия, отсутствие напарника/подходящего круга | 5, 8 |
| UI | Cursor/zoom/pan/reset, corner selection, touch, High-DPI, 375–1440 px, accessibility, loading/error/partial | 5–11 |
| Sharing | 15 вариантов рендера, русский текст, стабильный permalink, новый браузер, OG, обновление исходного dataset | 9 |
| Нагрузка/доступ | Cache hit, размер ответа, worker restart, API validation/rate limit, публичное чтение и ограниченная запись | 1–11 |
| Регрессия | LIVE, Replay, навигация, существующие share-карточки и сезонные данные | 11 |

После изменений кода запускать доступные проверки из `package.json`:

```sh
pnpm lint
pnpm typecheck
pnpm build
```

Единого скрипта `pnpm test` на дату плана нет. Для нового модуля использовать существующий подход `node --test` и добавить понятную команду для telemetry tests при их появлении. Запускать соответствующие тесты изменённых областей; для регрессии доступны, в частности, `pnpm test:live`, `pnpm test:openf1-results`, `pnpm test:share-links` и `pnpm test:historical-seasons`.

Точечные тесты выполняются в своём этапе; перед публичной приёмкой — общий набор и браузерная проверка. Если проверка заблокирована окружением или исходной ошибкой проекта, это записывается отдельно и не выдаётся за успешный результат.

## 19. Соответствие разделам исходного ТЗ

Таблица покрывает все разделы 1–85; конкретные задачи и критерии находятся в указанных этапах.

| Разделы ТЗ | Что покрывается | Этапы плана |
| --- | --- | --- |
| 1–2 | Цель и пользовательский путь | 0, 5, 8, 11 |
| 3–5 | Источники, хранение, внутренняя модель | 0–2 |
| 6–7 | Distance normalization и delta | 3 |
| 8 | Входная страница | 5 |
| 9.1–9.2 | Lap Compare / Best Lap Compare | 4–5 |
| 9.3–9.5 | Session / Teammate / Track Evolution | 8 |
| 10–11 | Основной экран и верхняя панель | 5 |
| 12–13 | Track Map и Driver Advantage Map | 6 |
| 14–17 | Каналы, синхронизация, zoom и повороты | 5–6 |
| 18–23 | Метрики, торможения, газ, повороты, Insights, summary | 7 |
| 24–25 | Вкладки и различимость пилотов | 5 |
| 26–33 | Share, форматы, варианты, рендер, permalink, OG, схема | 1, 9 |
| 34–39 | Сессии, пилоты, круги, validity, шины, гонка | 2, 4–5 |
| 40 | Будущие race extensions | 1; отложенный scope |
| 41–42 | Эволюция и погодный контекст | 2, 8 |
| 43–44 | Производительность и кеш | 2–4, 10 |
| 45–48 | Загрузка, ошибки, mobile и desktop | 5, 11 |
| 49–50 | URL и SEO | 5, 8–10 |
| 51–56 | Интеграция, live compatibility, OpenF1Provider, attribution и provenance; второй provider/fallback отложен по уточнению пользователя | 0–2, 6, 9 |
| 57–58 | Flags и события аналитики | 10 |
| 59 | Узнаваемость share-карточек | 9 |
| 60–62 | Компоненты, движок графиков, расширение до 3–4 traces | 1, 5–6 |
| 63–68 | Быстрый путь, presets, рекомендации, empty и recent | 5, 8 |
| 69 | Будущее избранное | 1, 9; аккаунтный UI отложен |
| 70–71 | Track metadata и canonical geometry | 1, 3, 6 |
| 72–76 | Pipeline, cleaning, API, готовый response, серверные расчёты | 2–4, 7 |
| 77–80 | Тесты, крайние случаи, pit laps, track status | 0, 3–4, 8, 11 |
| 81–82 | Визуальный стиль и исключённые функции | 5, 11; границы scope |
| 83 | Шесть фаз разработки | Этапы 1–9, таблица последовательности |
| 84 | Полная публичная готовность | 11, checklist ниже |
| 85 | Ответ о выигранном/потерянном времени | 3, 6–9 |

## 20. Definition of Done первой версии с подпиской

Полный сценарий проверяется от имени пользователя с активной подпиской на поддерживаемой сессии с доступными каналами. Отдельно проверяются гость, пользователь без подписки с доступом только к демо и корректная деградация на неполных данных.

- [ ] 01. Открыть Telemetry Hub.
- [ ] 02. Выбрать сезон.
- [ ] 03. Выбрать Grand Prix.
- [ ] 04. Выбрать сессию.
- [ ] 05. Выбрать двух пилотов.
- [ ] 06. Автоматически сравнить их лучшие круги.
- [ ] 07. Выбрать конкретные круги.
- [ ] 08. Увидеть time delta.
- [ ] 09. Увидеть speed.
- [ ] 10. Увидеть throttle.
- [ ] 11. Увидеть brake.
- [ ] 12. Увидеть gear.
- [ ] 13. Увидеть RPM.
- [ ] 14. Увидеть DRS.
- [ ] 15. Увидеть интерактивную карту трассы.
- [ ] 16. Увидеть, на каких участках кто быстрее.
- [ ] 17. Выбрать поворот и приблизить telemetry.
- [ ] 18. Увидеть основные RaceSide metrics.
- [ ] 19. Открыть автоматические RaceSide Insights.
- [ ] 20. Использовать Teammate Compare.
- [ ] 21. Использовать Session Compare.
- [ ] 22. Использовать Track Evolution.
- [ ] 23. Получить permalink на comparison.
- [ ] 24. Сгенерировать share-карточку.
- [ ] 25. Открыть то же сравнение по share-ссылке.

## 21. Журнал выполнения

Реализация ниже относится к новому Hub. Полная публичная приёмка ещё не подтверждена. Отчёт по текущему интерфейсу: [docs/telemetry/README.md](docs/telemetry/README.md).

| Этап | Статус | Подтверждение готовности / оставшиеся ограничения |
| --- | --- | --- |
| 0 — аудит | В работе | Реальный контрольный набор OpenF1 Monza 2026; широкая матрица сессий ещё не проверена |
| 1 — контракты | Реализовано локально | Типы, кэш, миграция и очередь; RLS проверена в изолированном PostgreSQL |
| 2 — загрузка | Реализовано локально | OpenF1Provider, каталог, кэш и обработка заданий |
| 3 — нормализация | Реализовано локально | Cleaning, интегрирование скорости, delta, пропуски; численные тесты |
| 4 — API | В работе | Каталог, сравнения, очередь, ссылки; отдельный просмотр pit lap ещё не завершён |
| 5 — основной экран | Проверено локально | Экран без прокрутки страницы и подвала; пять размеров Chromium |
| 6 — карта | Проверено локально | Исправлена плоскость 14 карт; повороты и преимущество; границы анализа приблизительные |
| 7 — аналитика | Реализовано локально | Метрики, сегменты, анализ поворотов и rule-based insights |
| 8 — режимы | В работе | Пять режимов реализованы; полная матрица реальных данных не пройдена |
| 9 — sharing | В работе | Ссылки, OG, варианты изображения; визуальная приёмка всех экспортов не завершена |
| 10 — готовность к эксплуатации | В работе | Flags, события, rate limits, sitemap; production-метрики не измерены |
| 11 — приёмка | Не завершён | Локальные проверки пройдены частично; production не изменялся |

После каждого этапа записывать выполненные задачи, реальные команды проверок и их результаты, подтверждённое покрытие, ограничения и следующий доступный этап.
